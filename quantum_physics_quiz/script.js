var startButton = document.getElementById('start')
var qQuestions = document.getElementById()
var qAnswers = document.getElementByclass(btn)



startButton.addEventListener('click', showResults);

array.forEach(element => {
    
});